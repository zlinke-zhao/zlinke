# OpenWorker 体验笔记（草稿·待站长亲测）

> 说明：本篇基于官网、GitHub 官方仓库 README 与公开独立评测聚合撰写，来源均已标注。**站长尚未亲自安装实测**，凡标注「官方口径」「社区反馈」处均未经本站独立验证。

- **收录日期**：2026-08-05
- **工具 ID**：`openworker`
- **官网**：https://openworker.com
- **仓库**：https://github.com/andrewyng/openworker（MIT 协议）
- **分类**：AI 工作台 / 桌面智能体
- **当前评分**：3.9

---

## 一、它是什么，解决什么问题

美国时间 2026 年 7 月 23 日，吴恩达（Andrew Ng）与 Rohit Prasad 在 X 上发布开源桌面 Agent **OpenWorker**。仓库首次导入是 7 月 22 日（从 `andrewyng/aisuite` 的 `platform/` 目录 hoist 出来独立成库），截至 8 月 2 日已有 115 次提交，8 月 1 日刚合入 Skills 支持——**仍在高频迭代**。

它想解决的问题，吴恩达在 README 里说得很直白：**「交付成品，不是聊天」**（delivers finished work, not just chat）。你要的是一份润色好的文档、一条带数据的 Slack 回复、一个整理干净的日历，而不是一串待办清单。

官网给的四个内置场景很能说明定位：销售（HubSpot+邮件+日历，发送前必须审批）、行政助理（收件箱分诊+日程托管）、市场（花费/归因/周一例会报表）、运维值班（Slack 告警分诊+查 runbook+起草事故记录）。

架构上分三层（据 AI 科技评论拆解与官方 README 示意图一致）：

| 层 | 内容 |
|---|---|
| 桌面界面 | 原生 shell + GUI，创建任务、看模型思考过程与工具调用记录 |
| 本地 Agent 服务 | Python 引擎，管任务循环、记忆、权限审批，**构建在 aisuite 之上** |
| 模型与连接器 | 任意模型 provider + 25+ 连接器 + 你的本地文件与终端 |

---

## 二、和库内已收录工具的差异点

这是我认为值得收录的核心理由——它**不是 OpenClaw 系的又一个发行版**，血统完全独立。

| 对比对象 | OpenWorker 的差异 |
|---|---|
| StepClaw / LobsterAI / DuMate / AutoClaw / ArkClaw（OpenClaw 系） | 全都是 OpenClaw 生态的产品化发行版，内核同源；OpenWorker 引擎建在吴恩达团队自己的 **aisuite** 上，是另一条独立技术路线 |
| Rowboat（开源本地优先） | Rowboat 第一性原理是「本地知识图谱长期记忆」；OpenWorker 的第一性原理是「**模型无关 + 审批门**」，记忆不是它的主打 |
| QwenPaw（阿里 AgentScope 开源） | QwenPaw 的远程指挥面向国内 IM（钉钉/飞书/微信）；OpenWorker 的连接器全是欧美 SaaS 栈（Slack/HubSpot/Jira/Linear/monday.com），**中文办公生态完全缺席** |
| JiuwenSwarm（华为开源） | 九问主打多 Agent 蜂群协同 + HITS 人入队；OpenWorker 是单 Agent，不做编排 |
| Claude Cowork / ChatGPT Work | 二者都绑死自家模型与云生态；OpenWorker 刻意反绑定，**BYO Key，平台方一分钱不收** |
| 爱派 AiPy（Python-Use） | AiPy 不预置工具、现场写 Python 执行，能力上限=Python 生态；OpenWorker 是 25+ 预制连接器 + 强制审批门，路线正好相反 |
| 千问办公 / 腾讯 WorkBuddy | 一体化封闭商业产品，厂商替你吸收所有复杂度；OpenWorker 把控制权和维护成本一起交还给你 |

**最独特的一点**：Slack 里 `@OpenWorker`，任务实际在**你自己的桌面机器上**跑（用你的文件、你的 Key），跑完把结果回帖到原线程。这个「云端触发 / 本地执行 / 云端回帖」的闭环，库内其他工具没有一模一样的做法。

---

## 三、独立评测暴露的真实短板（重点，别被顶流背书迷惑）

AI 科技评论（雷峰网系）2026-08-03 发了一篇标题很不客气的评测：《吴恩达开源的 OpenWorker，为什么「不 Work」了？》。这是目前最有价值的一手批评，摘录其核心发现：

**1. 「模型无关」有幻觉成分 —— API 兼容 ≠ 开箱即用**
- 社区反馈：接入自建 OpenAI-compatible 接口，配置页连接检测能通过，但进新会话后显示「没有可用模型」
- 社区反馈：本地 Ollama 服务本身正常，OpenWorker 却加载不出其中的模型
- 记者实测：模型账户额度不足时，系统**连续返回三次相同错误**，既不自动停止，也不提示换模型或查余额
- 该文结论很扎心：「BYOM 听起来是把自由还给用户，但实际上是把排错的灾难抛给了用户」

**2. 「本地优先」不等于数据流已说清**
数据实际分三类：存本机的会话/密钥/任务数据、登录云账户后可能上报的使用元数据、调用云模型与第三方连接器时必然外发的数据。社区关注的不是它是否够安全，而是**项目有没有清晰告诉用户哪类数据在哪种配置下去了哪里、能否关闭**。官方目前只说明了「默认架构倾向」。

**3. 权限审批可能存在旁路**
社区报告指出：某些配置文件可能在用户**尚未信任工作区时**就启动其中定义的外部服务进程。原文明确说明这只是社区报告与复现讨论，不代表已发生大规模攻击，也未证实所有版本受影响。但它点出了桌面 Agent 的通病——权限控制不能只覆盖「模型主动调工具」的主流程，还得覆盖配置解析、插件加载、连接器初始化、后台进程启动这些旁路。

**4. 工程完成度的硬伤（官方 README 自述）**
- 明确标注 **Open Beta**，会自动更新
- **Windows 构建尚未代码签名**，SmartScreen 会弹告警（签名"进行中"）
- **没有 Linux 版**
- macOS 仅 Apple Silicon 有直接下载（Intel 版走 CI 构建）

---

## 四、我给 3.9 分的理由

加分：独立技术血统（aisuite）、MIT 真开源可审计、模型选择权与数据控制权确实交还给用户、审批门设计思路正确、迭代频率高。

扣分：Beta 阶段、Windows 未签名、无 Linux、中文办公生态零覆盖（无钉钉/飞书/企微/微信，国内用户是硬伤）、BYO Key 抬高了普通办公用户的上手门槛、且已有三类社区实证问题未闭环。

一句话：**它是给能自己搞定 API Key 和排错的人用的开源参考实现，不是给财务小张开箱即用的数字员工。** AI 科技评论那句话说得挺准——「开放源码不等于交付产品」。

---

## 五、待站长亲测清单

1. Windows 11 上装一遍，实测 SmartScreen 告警的实际拦截强度与绕过步骤
2. 接 DeepSeek API（本站常用）验证「模型无关」在国产模型上是否真能跑通工具调用
3. 接本地 Ollama，验证社区反馈的「模型加载不出来」是否已修复
4. 实测审批门：故意让它执行一条危险 Shell 命令，看是否 100% 拦下
5. 无连接器场景下（不接 Slack/Gmail），纯本地文件整理任务的完成度如何
6. 额度耗尽时的错误处理是否已改进（对应评测里连续三次同错误那条）
7. 定时任务（晨报）连跑一周的稳定性

---

## 信息来源

- 官网 openworker.com（2026-08-05 抓取）
- GitHub andrewyng/openworker README（MIT，Beta 声明，下载与架构说明，2026-08-05 抓取）
- AI 科技评论《吴恩达开源的 OpenWorker，为什么「不 Work」了？》2026-08-03，作者李娜、编辑岑峰
- 52ai.com、ai-bio.cn、aiboss88.com、jxxy.net 等工具站产品介绍（用于交叉核对功能清单）

**安全验证记录**：URL 安全脚本首次判 high（`SSL handshake timed out`），三次重试均为 `safe=true / risk_level=low / 0 warnings`，判定为沙箱网络抖动误报（与 2026-07-11 Typewise 同因）。域名 HTTPS + `.com`，非高风险 TLD。
